export const Icons = {
    logo: ({ className }) => (
        <div className={`font-black text-2xl bg-gradient-to-r from-indigo-500 to-purple-500 bg-clip-text text-transparent ${className}`}>
            DS
        </div>
    )
};
